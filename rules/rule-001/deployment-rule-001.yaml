# SPDX-License-Identifier: Apache-2.0
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rule-002-rel-1-0-0
  namespace: processor
  labels:
    app: rule-002
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rule-002
  template:
    metadata:
      name: rule-002-rel-1-0-0
      labels:
        app: rule-002
      annotations:
        prometheus.io.scrape: 'false'
    spec:
      containers:
        - name: rule-002-rel-1-0-0
          image: docker.io/batzorig825/rule-002-rel-1-0-0-dev:1.0.0
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources: {}
          envFrom:
            - configMapRef:
                name: rule-002-configmap
          imagePullPolicy: Always
        - name: rule-002-sidecar-rel-1-0-0
          image: docker.io/batzorig825/sidecar-rel-1-0-0:1.0.0
          ports:
            - name: http
              protocol: TCP
              containerPort: 5000
          resources: {}
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      imagePullSecrets:
        - name: frmpullsecret
      schedulerName: default-scheduler
      enableServiceLinks: false
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
